<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             mc:Ignorable="d"
             x:Class="FBLAManager.Views.Events.MeetingDetailPage">

    <ContentPage.Content>
            
        <StackLayout Spacing="20" Padding="15">
            
            
            <Label Text="{Binding EventName}" d:Text="Meeting name" FontSize="Large" TextColor="Black" HorizontalTextAlignment="Center"/>

            <Label Text="{Binding From, StringFormat='{0:hh:mm tt}'}" TextColor="DarkSlateGray" FontSize="16"/>

            <Label Text="{Binding To, StringFormat='{0:hh:mm tt}'}" TextColor="DarkSlateGray" FontSize="16"/>

            <Label Text="{Binding Description}" d:Text="Meeting description" TextColor="DarkSlateBlue" FontSize="Medium" />

            <Button Text="Sign In" Pressed="SignUp_Pressed"/>

            <Label Text="Member(s) Attending" TextColor="Black" FontSize="Medium"/>
            
            <syncfusion:SfListView x:Name="MeetingAttendees" 
                   ItemsSource="{Binding MeetingAttendees}"
                   ItemSize="30">

                <syncfusion:SfListView.ItemTemplate>
                    <DataTemplate>

                            <Frame x:Name="frame" CornerRadius="3" 
                            BackgroundColor="White"
                            OutlineColor="LightGray"
                            Margin="0,4,0,0"
                            Padding="10,5,0,0">
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding Member.FirstName}" FontAttributes="Bold" TextColor="DarkBlue" FontSize="12" />
                                    <Label Text="{Binding Member.LastName}" FontAttributes="Bold" TextColor="DarkBlue" FontSize="12"/>
                                </StackLayout>
                            </Frame>
                    </DataTemplate>
                </syncfusion:SfListView.ItemTemplate>
            </syncfusion:SfListView>
            
        </StackLayout>

        


    </ContentPage.Content>
</ContentPage>